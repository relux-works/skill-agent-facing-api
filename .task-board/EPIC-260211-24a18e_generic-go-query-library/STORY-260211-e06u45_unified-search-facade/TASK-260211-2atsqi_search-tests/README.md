# TASK-260211-2atsqi: search-tests

## Description
Tests for scoped grep: simple match, case-insensitive, file glob filter, context lines with IsMatch distinction, regex patterns, empty results, invalid regex error. Create temp test directory with sample .md files.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
