# TASK-260213-3sy7kc: implement-count-operation

## Description
Add count() as a new operation on Schema. Accepts same filter params as list() (status, assignee, etc). Returns {"count": N} with total matching items. Does NOT return items themselves. Reuse item loading and filtering logic from list().

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
