# STORY-260213-ajyfs9: list-count-operation

## Description
Add count() operation or count parameter for list() that returns the total number of matching items without returning the items themselves. Essential for pagination — caller needs to know total count to calculate pages. Should support the same filters as list().

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
