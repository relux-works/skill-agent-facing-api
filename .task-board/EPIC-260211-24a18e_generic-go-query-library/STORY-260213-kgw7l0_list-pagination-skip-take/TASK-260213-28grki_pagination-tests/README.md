# TASK-260213-28grki: pagination-tests

## Description
Tests for skip/take pagination: basic skip, basic take, skip+take combo, skip past end (empty), take more than available, skip=0 (noop), negative values (error), works with filters, works with field projection, works with compact output.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
