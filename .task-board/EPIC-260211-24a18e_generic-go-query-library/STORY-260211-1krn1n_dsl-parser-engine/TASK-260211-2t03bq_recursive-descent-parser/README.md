# TASK-260211-2t03bq: recursive-descent-parser

## Description
Implement recursive descent parser. Grammar: batch = query (';' query)*, query = operation '(' params ')' ['{' fields '}'], params = param (',' param)*, param = key '=' value | value, fields = ident+. Accept ParserConfig with optional Operations map and FieldResolver interface.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
