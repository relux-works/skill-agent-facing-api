# TASK-260211-30hc1s: field-selector-apply

## Description
Implement FieldSelector[T] with Apply(item T) map[string]any, Include(field) bool, Fields() []string. Expand presets, validate fields, lazy evaluation.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
