# TASK-260214-2pjbph: research-declarative-filter-registration

## Description
Design a declarative API for registering filterable fields on Schema. Options: FilterableField() with type+comparators, auto-extraction from FieldAccessor, filter middleware chain. Key questions: how to type-safely register comparisons for string vs int vs enum fields, how to compose with existing Args-based approach.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
