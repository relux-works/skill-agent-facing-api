# TASK-260213-uzvohq: add-operation-with-metadata

## Description
Add OperationWithMetadata(name, handler, meta) method to Schema. Stores handler in operations map (as before) + metadata in new operationMetadata map. Backwards compatible — existing Operation() works unchanged.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
