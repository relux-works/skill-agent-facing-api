# STORY-260211-1krn1n: dsl-parser-engine

## Description
Generic tokenizer + recursive descent parser + AST. Reusable across domains — user doesn't touch parser code, just registers operations. Supports batch queries via semicolons, field projection syntax, positional and key=value args.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
