{
  "mode": "local",
  "local": {
    "board_dir": ".task-board"
  },
  "version_control": {
    "confirm": true,
    "desired_commit_time": "backdated to previous day, after 20:00 MSK (evening) per owner policy"
  },
  "spawn": {
    "enabled": true,
    "max_parallel": 20,
    "ceilings": {
      "codex": {
        "model": "gpt-5.6-sol",
        "model_criterion": "less_or_equal",
        "reasoning_effort": "xhigh"
      },
      "claude": {
        "model": "claude-fable-5",
        "model_criterion": "less_or_equal"
      }
    },
    "preferred_agentic_system": {
      "mixed": [
        "claude",
        "codex"
      ]
    },
    "worktree_isolation": {
      "integration_base_branch": "main",
      "validation": {
        "commands": [
          "cd agentquery && go vet ./...",
          "cd agentquery && test -z \"$(gofmt -l .)\"",
          "cd agentquery && go test ./... -count=1",
          "cd example && go vet ./..."
        ]
      }
    }
  }
}
