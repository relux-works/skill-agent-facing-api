# STORY-260211-36v125: field-registry-and-projection

## Description
Core building block: generic field registry where user defines fields (name -> accessor func), presets (name -> field list), and gets a Selector that applies projection to any domain object. Everything else depends on this.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
