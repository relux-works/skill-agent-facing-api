# STORY-260212-3edjqt: compact-serializer

## Description
Имплементация компактного сериализатора в agentquery. Новый output mode (помимо JSON) — табличный/TOON-подобный формат для Schema.QueryJSON(). Schema-once + values-only для однородных списков. Fallback на JSON для nested/heterogeneous данных.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
