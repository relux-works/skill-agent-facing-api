# STORY-260214-39yoof: distinct-groupby-helpers

## Description
Pure generic helpers (Distinct, DistinctCount, GroupBy) + auto-registered distinct DSL operation that reuses FilterableField accessors. ~225 LOC including tests.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
