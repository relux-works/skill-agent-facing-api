# TASK-260212-kvy4rk: evaluate-hybrid-approach

## Description
Проработать гибридный подход: табличный формат для list() с однородными записями, JSON для get() с nested данными, compact text для summary(). Матрица: operation type → best format. Учесть что agentquery уже знает schema — можно header генерить автоматом.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
