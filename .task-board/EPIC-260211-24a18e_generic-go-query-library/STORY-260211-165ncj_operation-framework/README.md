# STORY-260211-165ncj: operation-framework

## Description
Operation registration and dispatch. User registers handler functions for operations (get, list, summary, custom). Framework dispatches parsed AST statements to handlers, applies field projection, handles batch errors. Built-in operations optional (user can implement all custom).

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
