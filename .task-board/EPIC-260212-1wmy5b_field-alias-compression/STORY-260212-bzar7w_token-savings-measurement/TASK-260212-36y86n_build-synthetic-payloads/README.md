# TASK-260212-36y86n: build-synthetic-payloads

## Description
Generate synthetic compact output datasets for benchmarking. Create payloads at 4 scales: 5, 20, 100, 500 items. Each payload in 3 variants: (a) JSON with full field names, (b) compact tabular with full field names, (c) compact tabular with abbreviated field names (1-2 char aliases). Fields: id→i, name→n, status→s, assignee→a, description→d, priority→p, created→c, updated→u. Store in .research/synthetic-payloads/.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
