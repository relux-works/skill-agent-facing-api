# TASK-260211-12cog0: scoped-grep-implementation

## Description
Implement scoped grep in search.go. Walk data directory with extension filter. Regex matching per line. Return []SearchResult with Source{Path, Line}, Content, IsMatch. Support FileGlob, CaseInsensitive, ContextLines options. Paths relative to data dir.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
