# TASK-260213-3bst9i: parse-skip-take-params

## Description
In operation handlers, extract skip/take integer params from ctx.Statement.Args. Add helper or convention for parsing int params. skip defaults to 0, take defaults to all (no limit). Validate: skip >= 0, take > 0.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
