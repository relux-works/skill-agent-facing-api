# TASK-260213-2osrnu: schema-introspection-tests

## Description
Tests: schema() with no metadata (backwards compat), schema() with metadata for some ops, schema() with metadata for all ops, OperationMetadata fields serialized correctly, ParameterDef defaults/optionals.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
