# TASK-260213-2jv2ub: update-introspect-output

## Description
Update schema.introspect() to include operationMetadata in the output map. Only include operations that have metadata registered. Operations without metadata appear in operations list but not in operationMetadata.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
