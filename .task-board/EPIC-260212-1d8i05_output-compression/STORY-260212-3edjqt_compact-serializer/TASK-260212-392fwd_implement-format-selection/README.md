# TASK-260212-392fwd: implement-format-selection

## Description
Логика автовыбора формата: list() с N>1 однородных записей → табличный, get() с nested → JSON, summary() → compact text. Fallback на JSON если данные не ложатся в табличный формат.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
