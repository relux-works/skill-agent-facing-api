# TASK-260906-1auqyl: adopt-and-land-the-dsl-error-guidance-change-and-release-v1-6-0

## Description
The change already exists, written and gated by a task-board producer: a 7-file delta to agentquery/ (error.go, grammar.go, grammar_test.go, parser.go, query.go, render.go, schema.go; 469 insertions, 37 deletions) on branch bug/BUG-260903-12avv2-dsl-error-guidance in the Git worktree /Users/alexis/src/relux-works/skill-project-management/.temp/BUG-260903-12avv2/agentquery-worktree, based on this repository main 656ad0a. The same delta is exported as the consumer board resource file:///Users/alexis/src/relux-works/skill-project-management/.task-board/.resources/BUG-260903-12avv2/BUG-260903-12avv2_agentquery.patch. What it adds (all additive, no removed or changed public signatures): Grammar/DSLGrammar/GrammarSyntax() exposing the statement grammar (argument key=value syntax, quoting, projection braces, batch separator) from schema(); NewUnknownOperationError(name, pos, known) and IsUnknownOperation; ParseError gains Operation and OperationPos so an unknown operation with unparsable arguments still reports unknown-operation with the known-operation list instead of a positional error; render.go renders ASTs back to DSL for the grammar text; grammar_test.go asserts the grammar text is derived from the parser and drift-tested. Adopt it: apply the delta onto current main in your workspace (git apply --3way of the patch, or cherry-pick the worktree state), read every hunk as a reviewer would, run go build ./..., go vet ./..., gofmt -l, go test ./... -count=1 across the module, and record the exact API surface added in the results note. If anything in the delta is wrong, fix it here and say what changed relative to the exported patch. Then hand off to review. After review acceptance the orchestrator lands it on main and tags agentquery/v1.6.0; do not tag or push from this run.

## Scope
(define task scope)

## Acceptance Criteria
1. The 7-file agentquery delta from the consumer worktree applies onto current main and every hunk has been read; any deviation from the exported patch is named in the results note. 2. go build ./..., go vet ./..., gofmt -l and go test ./... -count=1 pass for the agentquery module, run in the foreground, outputs quoted. 3. The added public API surface is listed exactly (GrammarSyntax, NewUnknownOperationError, IsUnknownOperation, ParseError.Operation and OperationPos, Grammar/DSLGrammar) and confirmed additive: no existing exported signature changed or removed. 4. grammar_test.go proves the grammar text is derived from the parser (drift test) and the unknown-operation-with-unparsable-arguments case reports the known-operation list plus a schema() pointer. 5. Results note attached as a task-scoped resource, work left uncommitted, handoff via task-board handoff TASK-260906-1auqyl --role developer; no tag, no push from the run.
