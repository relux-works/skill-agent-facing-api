# TASK-260212-fgxx71: design-output-format-api

## Description
Спроектировать API для выбора output формата. Варианты: флаг в QueryOptions (format=json|compact|toon), отдельный метод QueryCompact(), автовыбор по типу операции. Учесть обратную совместимость — JSON остаётся дефолтом.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
