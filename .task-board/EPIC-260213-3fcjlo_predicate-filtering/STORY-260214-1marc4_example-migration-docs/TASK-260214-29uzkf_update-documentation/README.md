# TASK-260214-29uzkf: update-documentation

## Description
Update project documentation:
- CLAUDE.md: Add FilterableField, SortField, Distinct API docs. Update Architecture section with new files (filter.go, sort.go). Update DSL Grammar with sort_ convention. Add pipeline description (Filter → Sort → Paginate → Project). Update example commands.
- README.md: Update if needed with new features.
- assets/query-patterns-catalog (if exists): Add filter/sort/distinct patterns.

DEPENDS ON: all implementation tasks complete.
Definition of Done: docs accurate, all example commands work.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
